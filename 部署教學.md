# 📖 交班小幫手 部署教學

> 完全不懂程式也沒關係，照著步驟一步一步來！

---

## 📋 目錄
1. 申請 LINE Bot 帳號
2. 申請 GitHub 帳號並上傳程式
3. 部署到 Railway（免費雲端主機）
4. 設定環境變數
5. 把 Bot 加入群組
6. 測試

---

## STEP 1：申請 LINE Bot 帳號

1. 開啟瀏覽器前往：https://developers.line.biz/
2. 點右上角「Log in」，用你的 LINE 帳號登入
3. 登入後點「Create a new provider」
   - Provider name 輸入：`日照中心` （可自訂）
   - 點 Create
4. 點「Create a new channel」→ 選「Messaging API」
5. 填寫資料：
   - Channel name：`交班小幫手`
   - Channel description：`日間照顧中心交班系統`
   - Category：選 `Healthcare`
   - Subcategory：隨意選一個
   - Email：填你的信箱
6. 勾選同意條款 → 點 Create

7. 進入剛建立的 Channel，找到以下兩個重要資訊（先複製存起來）：
   - **Channel secret**（在 Basic settings 頁面）
   - **Channel access token**（在 Messaging API 頁面，點 Issue 產生）

8. 在 Messaging API 頁面：
   - 把「Allow bot to join group chats」**打開**
   - 把「Auto-reply messages」**關閉**
   - 把「Greeting messages」**關閉**

---

## STEP 2：申請 GitHub 並上傳程式碼

1. 前往 https://github.com 註冊帳號（免費）

2. 登入後點右上角「+」→「New repository」
   - Repository name：`linebot-daycare`
   - 選 **Public**
   - 點 Create repository

3. 把本資料夾的所有程式碼上傳：
   - 點「uploading an existing file」
   - 把以下檔案全部拖進去：
     - `app.js`
     - `package.json`
     - `.gitignore`
     - `.env.example`
     - `db/database.js`
     - `routes/message.js`
     - `routes/scheduler.js`
   - 點「Commit changes」

---

## STEP 3：部署到 Railway

1. 前往 https://railway.app
2. 點「Login」→「Login with GitHub」（用剛才的 GitHub 帳號登入）
3. 點「New Project」→「Deploy from GitHub repo」
4. 選擇你剛才建立的 `linebot-daycare`
5. Railway 會自動開始部署（等約 1-2 分鐘）
6. 部署完成後，點「Settings」→「Networking」→「Generate Domain」
   - 會產生一個網址，例如：`https://linebot-daycare-xxxx.railway.app`
   - **把這個網址複製起來！**

---

## STEP 4：設定環境變數

在 Railway 專案頁面：
1. 點「Variables」分頁
2. 點「New Variable」，逐一加入以下兩個：

| Variable Name | Value |
|---------------|-------|
| `LINE_CHANNEL_ACCESS_TOKEN` | 剛才複製的 Channel access token |
| `LINE_CHANNEL_SECRET` | 剛才複製的 Channel secret |

3. 加完後 Railway 會自動重新啟動

---

## STEP 5：設定 LINE Webhook

1. 回到 LINE Developers Console
2. 進入你的 Channel → Messaging API 頁面
3. 找到「Webhook URL」
4. 輸入：`https://你的railway網址/webhook`
   - 例如：`https://linebot-daycare-xxxx.railway.app/webhook`
5. 點「Verify」→ 如果出現「Success」代表成功！
6. 把「Use webhook」**打開**

---

## STEP 6：把 Bot 加入群組

1. 在 LINE Developers → Messaging API 頁面
2. 有一個 QR Code，用 LINE 掃描加入 Bot 為好友
3. 開啟你們的交班群組
4. 點右上角「邀請」→ 找到「交班小幫手」加入群組

---

## STEP 7：測試

在群組裡輸入以下指令測試：

```
Help
→ 應該出現使用說明

紀錄 測試王杯杯今日狀況良好
→ 應該回覆「✅ 紀錄成功！」

查詢
→ 應該顯示剛才的紀錄

刪除 1
→ 應該刪除第1筆
```

---

## ⏰ 自動推播時間確認

| 時間 | 動作 |
|------|------|
| 每日 07:00 | 推播昨日交班內容給早班 |
| 每日 07:01 | 自動清除昨日紀錄 |
| 每日 18:30 | 推播今日交班預覽供確認 |

---

## ❓ 常見問題

**Q：Bot 沒有回應？**
→ 確認 Webhook URL 設定正確，且「Use webhook」有打開

**Q：推播沒有收到？**
→ 確認 Bot 有被加入群組，且有人在群組說過話（這樣 Bot 才會記錄群組ID）

**Q：時間不對？**
→ 確認 Railway 的 timezone 設定，程式已設定為 `Asia/Taipei`

---

## 📞 指令速查

| 指令 | 功能 |
|------|------|
| `紀錄 [內容]` | 新增交班事項 |
| `查詢` | 查看今日所有紀錄 |
| `刪除 [編號]` | 刪除指定紀錄 |
| `Help` | 顯示說明 |
